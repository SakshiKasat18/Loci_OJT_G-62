On-device storage (mobile, offline-first):
- pack_cache(pack_id, version, local_path, downloaded_at)
- play_history(poi_id, last_played_at, cooldown_until)
- confidence_state(last_anchor_id, confidence_value, state, updated_at)
- wifi_fingerprints(anchor_node_id, bssid, mean_rssi, variance, sample_count)